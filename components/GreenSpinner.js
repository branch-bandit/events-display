import React from 'react';
import { Container, Spinner } from 'native-base';

const GreenSpinner = () => (
	<Container>
		<Spinner color="green" style={{ paddingVertical: 300 }} />
	</Container>
);

export default GreenSpinner;
